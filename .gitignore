node_modules
dist
dist-ssr
*.local
.DS_Store
.vite

# Build info TypeScript
*.tsbuildinfo

# Outillage local (non lié à l'application)
.claude/
_bmad/
_bmad-output/
docs/

# Sources de données réelles / audit interne (ne pas publier — contiennent n° de série, audit)
*.xlsx
*.pptx
*.pdf
